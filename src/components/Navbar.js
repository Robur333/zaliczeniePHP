import React from 'react';

const Navbar = function () {
  return (
    <nav>
      <ul className="nav-container button">
        <button>Play New Game</button>
        <button>Scoreboard</button>
      </ul>
    </nav>
  );
};

export default Navbar;
